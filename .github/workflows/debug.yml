name: Debug with sshx

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    timeout-minutes: 355

    steps:
      - uses: actions/checkout@v4

      - name: Setup sshx session
        run: curl -sSf https://sshx.io/get | sh -s run
